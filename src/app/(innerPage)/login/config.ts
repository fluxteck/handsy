// Temporary feature flag: Google/Apple sign-in is fully built (see signUpForm.tsx /
// signInForm.tsx) but hidden from the UI for now. Flip this back to `true` to restore it —
// no other changes needed.
export const SOCIAL_LOGIN_ENABLED = false;
